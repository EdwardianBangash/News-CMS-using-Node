<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body>
    <div class="wrapper">
        <h1>Login here!</h1>
        <%- include('partials/message') %>
        <form action="/login" method="post">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" name="email" id="email" value="<%= typeof email != 'undefined' ? email : ''%>" placeholder="Please enter your email address">
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" name="password" value="<%= typeof password != 'undefined' ? password : ''%>" id="password" placeholder="Please enter your password">
            </div>

            <input type="submit" value="Login">
        </form>
        <div class="note">Dont have an account <a href="/register">register here</a></div>
    </div>
</body>
</html>